/target
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# ik_llama writes this into the working directory.
llama.log

# Operator-specific model paths for the estimate dump script.
# The .example file is committed as a template; the real file is gitignored.
scripts/calibration/models.toml
